# 问答流式输出浏览器验证

日期：2026-09-08。使用本地生产构建和虚构演示账户，并在 Vercel 生产环境复验。

- 页面加载、首页、插画及知识问答入口正常，浏览器控制台无 error 或 warning。
- 提交“治疗期间疲劳应该记录什么？”后，页面立即显示用户问题和“正在核对相关依据…”。
- `POST /api/messages/stream` 返回 200；前端逐段写入助手消息，完成后显示两条经验证来源。
- 刷新页面并重新进入知识问答，问题、回答和来源仍存在，证明流式完成事件与数据库持久化一致。
- 自动测试 `new-app-tests.xml` 为 20 项通过，包含 NDJSON 事件顺序、回答内容和历史落库断言。

## 生产环境复验

- 生产地址：`https://apex-care-tan.vercel.app`；Vercel 部署状态为 Ready，`GET /api/health` 返回 `{"status":"ok"}`。
- Supabase 项目已更名为 `APEX Care`，9 张业务表全部启用 RLS；匿名与 authenticated 角色无表权限，应用仅由服务端数据库连接访问。
- 演示空间成功写入 1 个隔离用户、1 个会话、2 份文档和 2 个私有 Storage 对象。
- 线上问答立即显示“正在核对相关依据…”，随后显示 Kimi 回答和 2 条经验证来源；数据库保存 2 条消息，刷新后历史恢复。
- 最新生产部署未产生 error 级运行日志。`APEX_ALLOW_REAL_UPLOADS=false`，线上仅开放虚构演示资料。
