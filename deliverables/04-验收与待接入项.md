# 当前验收与待接入项

更新：2026-09-06。本文件用于核对目标完成情况，不把开发版通过测试等同于真实资料已上线。

| 用户要求 | 当前证据 | 判定 |
|---|---|---|
| 红色系、PC优先、手机自适应 | `designs/desktop`两张PC设计图；React/CSS实现；1440px与390px浏览器操作 | 已实现并做重点页面验证 |
| 单独生成小插画并还原到UI | `app/frontend/public/art`三个生成PNG；首页/空状态/问答实际使用 | 已实现 |
| 上传、核对、持续档案、照护、来源问答 | 服务端数据库、来源校验、任务版本；浏览器完成主流程 | 核心功能已实现 |
| 原有问题修复 | 增量上传、重核对保留状态、完成任务、DOCX表格等回归测试 | 新版已覆盖，原代码保留用于研究 |
| 机构数据检查、参数确认与ROI对照 | CSV检查、参数表、方案存档、情景/敏感性表；原计算内核 | 已实现上述范围；文献审核工作流未完整迁移 |
| 需要API的位置说明 | `app/.env.example`、README、技术方案 | 已交付；服务端配置已写入，密钥不进入前端或版本库 |
| OCR效果最好的选型 | PaddleOCR-VL-1.6适配、官方接口核对、离线评分工具 | 真实虚构资料联调成功；仍需代表性样本评测，不能认定最优 |
| 境内真实资料试用 | 配置默认关闭真实资料；技术架构和验收步骤 | 尚未完成云部署、权限审查、备份删除、供应商处理边界核验 |

本地测试证据：`verification/new-app-tests.xml`为19项通过；原项目`legacy-recheck.xml`为109项通过；前端TypeScript和Vite构建通过。单元测试的 OCR 响应与恢复采用模拟数据；独立真实测试报告为 `live-ocr.json`、`live-kimi.json`、`live-pdf-extraction.json`。浏览器问答实际返回回答及三条来源，已验证页面到服务链路。

## 后续可选工作（不阻塞本次交付）

补充完整链路验收：`verification/live-app-flow.json` 使用独立虚构账户，通过实际 HTTP 上传两页扫描 PDF，后台调用 PaddleOCR 和 Kimi，产生两条默认待核对事项。确认后生成任务，完成任务后撤回并重新确认仍保留完成状态；重复上传复用原资料；新客户端携带原会话读取状态保持；另一账户下载原件返回 404。测试结束删除两个测试账户。该证据覆盖本地服务链路，不代表生产云部署或医疗准确率评测。

1. 已接入百度托管 PaddleOCR-VL 和 LangGraph + Kimi K3，无需另填 AI Studio Access Token。运行 `.venv/Scripts/python.exe -m app.backend.check_config` 可检查配置是否存在；命令不联网、不输出密钥。
2. 指定一组获准评测的脱敏资料，以及关键日期、数值、单位等人工正确答案。明确这些资料可以发送到哪个OCR/模型服务，才能执行真实对照评测。
3. 如要开放真实资料：提供境内云部署目标、适用供应商数据处理条款和确定的保存/删除要求。将其落实为对象存储、数据库与备份生命周期，再做上线验收。

本地还可以扩展测试和页面，但这些工作无法代替上述输入。用户已取消额外的 OCR 准确率评测要求。本次 PC 优先、手机适配的界面、生成插画、核心功能及指定 API 接入交付完成；不宣称 PaddleOCR 在所有医疗资料上最优。境内生产部署与真实资料开放仍属于后续上线工作。


### 用户范围更新

2026-09-06：用户明确不再要求准确率评测，因此不再等待样本或人工标注。采用已接入并通过虚构资料完整链路验证的 PaddleOCR-VL，保留逐条核对功能。
